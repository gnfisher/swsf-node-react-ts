{
  "name": "swsf",
  "version": "1.0.0",
  "description": "Software for the swsfonline.com forums",
  "main": "build/index.js",
  "author": "Greg Fisher <greg@gnfisher.com>",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.1.2"
  },
  "scripts": {
    "start": "concurrently \"cd ./server && yarn install && yarn start\" \"cd ./client && yarn install && yarn start \"",
    "build": "rimraf ./sever/build && rimraf ./client/build && concurrently \"yarn tsc -b server\" \"cd client && yarn build\""
  }
}
